{% extends "notifications/base.mjml" %}
{% load notification_extras %}

{% block content %}
<mj-section background-color="#ffffff" padding="30px 20px">
  <mj-column>
    <mj-text font-size="20px" font-weight="bold" padding-bottom="10px">Reservation placed</mj-text>
    <mj-text>Hello {{ user_name }},</mj-text>
    <mj-text>
      You have been added to the queue for <strong>{{ entry_title }}</strong>{% if entry_author %} by {{ entry_author }}{% endif %}.
    </mj-text>
    <mj-text>
      Your position in line: <strong>#{{ position }}</strong>.
    </mj-text>
    {% if estimated_available_from and estimated_available_until %}
    <mj-text>
      Expected to become available between <strong>{{ estimated_available_from|isodatetime }}</strong> and <strong>{{ estimated_available_until|isodatetime }}</strong>.
      This is only an estimate: the book may come back earlier if the current reader returns it, or later if the loan is renewed.
    </mj-text>
    {% elif estimated_available_from %}
    <mj-text>
      Expected to become available after <strong>{{ estimated_available_from|isodatetime }}</strong>, when the current loan ends.
      This is only an estimate: the book may come back earlier if the current reader returns it, or later if the loan is renewed.
    </mj-text>
    {% endif %}
    <mj-text>
      We will email you again as soon as a slot opens up. You will have {{ claim_window_hours }} hours to claim it.
    </mj-text>
  </mj-column>
</mj-section>
{% endblock %}
